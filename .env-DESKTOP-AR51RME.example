# ===============================================
# MODO DE DESENVOLVIMENTO (Exemplo)
# Descomente e ajuste as linhas abaixo para rodar localmente.
# Salve este arquivo como ".env" na raiz do projeto.
# ===============================================

# --- Configurações Gerais ---
# Ativa o modo de depuração para ver erros detalhados. NUNCA use True em produção.
#DEBUG=True

# Chave secreta para desenvolvimento. Troque por uma string aleatória.
#SECRET_KEY=uma_chave_secreta_forte_para_desenvolvimento

# Hosts permitidos para acessar a aplicação em desenvolvimento.
#ALLOWED_HOSTS=localhost,127.0.0.1

# Origens permitidas para o frontend em desenvolvimento (ajuste a porta se necessário).
#CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000


# --- Banco de Dados (Opcional) ---
# Por padrão, o Django usará um arquivo SQLite (db.sqlite3) se estas variáveis não forem definidas.
# Para usar PostgreSQL com Docker, descomente e use as variáveis abaixo.
# POSTGRES_DB=compras
# POSTGRES_USER=user
# POSTGRES_PASSWORD=password
# POSTGRES_HOST=db # 'db' é o nome do serviço no docker-compose.yml


# ===============================================
# MODO DE PRODUÇÃO (Referência)
# Em produção, estas variáveis DEVEM ser definidas diretamente no ambiente do seu servidor/container.
# NÃO use um arquivo .env em um repositório de produção público.
# ===============================================

# --- Configurações Gerais (Produção) ---
# DEBUG=False
# SECRET_KEY=SUA_CHAVE_SECRETA_REALMENTE_LONGA_E_SEGURA_AQUI
# ALLOWED_HOSTS=seu_dominio.com,www.seu_dominio.com
# CORS_ALLOWED_ORIGINS=https://seu_frontend.com

# --- Banco de Dados (Produção) ---
# POSTGRES_DB=compras_prod
# POSTGRES_USER=user_prod
# POSTGRES_PASSWORD=senha_super_segura
# POSTGRES_HOST=db_prod # ou o endereço do seu banco de dados